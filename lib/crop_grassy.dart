import 'package:flutter/material.dart';

class Crop_grassy extends StatelessWidget {
  const Crop_grassy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Container(
        child: Text('its grassy crop'),
      ),
    ));
  }
}
