import 'package:flutter/material.dart';

class Crop_rocky extends StatelessWidget {
  const Crop_rocky({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Container(
        child: Text('its rocky crop'),
      ),
    ));
  }
}
